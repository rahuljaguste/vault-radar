node_modules/
.env
.env.*
!.env.example
dist/
.next/
target/
*.spkg
# …except the packed modules this repo builds and ships; the sink image and the
# published package URL both need the artifact itself, not a rebuild instruction.
!substreams/erc4626-vault-metrics/*.spkg
.DS_Store
*.log
.claude/
keys/
.worktrees/
.superpowers/
/runs/
substreams/**/target/
packages/dashboard/.next/
deck
render-deck.mjs

# The demo-video pipeline. A local production tool for making the submission video, not
# part of the project the repository is about, so it is not carried here.
scripts/video/
